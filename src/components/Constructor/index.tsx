import { Bar } from '../Bar';

export const Constructor = () => {
  return (
    <section className="constructor">
      <Bar name="display" />
      <Bar name="operators" />
      <Bar name="nums" />
      <Bar name="equal" />
    </section>
  );
};
